# Notes

This Zephyr app is a first complete firmware-side pass. It keeps the portal in one C file for early board bring-up. Once it builds and runs on hardware, split the app into modules for Wi-Fi, DNS, HTTP, storage, and page rendering.
